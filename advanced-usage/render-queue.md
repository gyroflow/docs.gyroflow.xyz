# ⏩ Render queue

```
    - Adding to render queue
    - Drag & drop to render queue
    - Dropping presets in auto queue
    - Autosync in render queue
    - Apply settings to all items in render queue
    - export mode and default overwrite action
    - Parallel renders
    - Actions after render queue is done (eg. shut down the computer)
    - Custom output directory (in the bottom field "Output path" before dragging files), when you drag multiple files to the render queue directly
```
